const initState = {
    gameState: {},
    socket: {},
    user: ""
}

const quizzlyReducer = (state=initState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default quizzlyReducer ;
